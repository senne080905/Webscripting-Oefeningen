let getallen = [2, 6, 78, 8];

// 1) met een foreach toon je elk getal

getallen.forEach(function(getal) {
    console.log(getal);
});

// 2) met een foreach toon je het dubbel van elk getal

getallen.forEach(function(getal) {
    console.log(getal * 2);
});